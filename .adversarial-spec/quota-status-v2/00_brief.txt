Quota Status Bar v2 — Hermes Plugin Enhancement

The ~/.hermes/plugins/hermes-quota-status/ plugin currently shows Claude, Codex, and Gemini quota in the Hermes TUI status bar. It needs:

1. Add GLM/智谱 (Z.AI Coding Plan) — uses POST /api/monitor/usage/quota/limit on api.z.ai or open.bigmodel.cn, auth via Authorization header with raw GLM_API_KEY or ZHIPU_API_KEY env var
2. Add DeepSeek (pay-per-token) — use GET https://api.deepseek.com/user/balance, auth via Bearer DEEPSEEK_API_KEY
3. Rename Gemini short code from G to Ge (GLM takes G)
4. Claude: show both 5h session AND 7-day weekly windows
5. Codex: show both primary AND secondary windows
6. Config-driven provider filtering via quota_status.providers in hermes config.yaml
7. Relative reset countdowns (3h42m) instead of absolute time
8. 60-char output trim for narrow terminals
9. Suppress providers after 3 consecutive auth failures

Target: ~/.hermes/plugins/hermes-quota-status/
4 source files: __init__.py, quota_api.py, test_quota_status.py, plugin.yaml
All 4 must be changed, with tests passing.
